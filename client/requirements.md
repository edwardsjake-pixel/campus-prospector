## Packages
date-fns | Date formatting and manipulation
react-day-picker | Calendar component for selecting dates
recharts | Charts for dashboard analytics
framer-motion | Animations for page transitions and interactions
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication is handled via Replit Auth (useAuth hook).
API endpoints should follow standard REST patterns: /api/instructors, /api/courses, etc.
Use shadcn/ui components pattern.
